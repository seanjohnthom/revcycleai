# Netlify Environment Variables
# Set these in: Netlify Dashboard → Site → Environment Variables

# MailerLite API key (from mailerlite.com → Integrations → API)
MAILERLITE_API_KEY=your_mailerlite_api_key_here

# MailerLite Group ID — optional but recommended
# Create a "RevCycleAI Newsletter" group in MailerLite, copy the ID here
# (mailerlite.com → Subscribers → Groups → click group → ID in URL)
MAILERLITE_GROUP_ID=your_group_id_here

# Stripe (for RevCycleAI Pro subscriptions)
# Get these from: stripe.com → Developers → API keys + Products
# Pricing: $19/mo or $179/yr (founding member rate - locked forever)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PRICE_MONTHLY=price_...   # $19/mo price ID (create new in Stripe)
STRIPE_PRICE_ANNUAL=price_...    # $179/yr price ID (create new in Stripe)
SITE_URL=https://revcycleai.com
